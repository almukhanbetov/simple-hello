# Используем официальный образ Go
FROM golang:1.24-alpine

# Устанавливаем рабочую директорию внутри контейнера
WORKDIR /app

# Копируем go-модули (если ты их используешь)
COPY go.mod ./
COPY go.sum ./
RUN go mod download

# Копируем исходный код
COPY . .

# Собираем Go-приложение
RUN go build -o main .

# Указываем порт, который приложение использует
EXPOSE 8085

# Команда запуска
CMD ["./main"]
